{% extends "base.jinja2" %}

{% block content %}
    {% include "nav.jinja2" %}
    <h1>User: {{ user.username }}</h1>
    {% for post in posts %}
        <p>
            {{ post.author.username }} says: <b>{{ post.body }}</b>
        </p>
    {% endfor %}
{% endblock %}