<p>
    <img src="{{ post.author.avatar(36) }}" alt="{{ post.author.username }}_avatar">
    <a href="{{ url_for('user', username=post.author.username) }}">
        {{ post.author.username }}
    </a> says: <b>{{ post.body }}</b>
</p>