<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        {% if title %}
        <title>{{ title }} - Flask project</title>
        {% else %}
        <title>Hello, world!</title>
        {% endif %}
    </head>
    <body>

        {% block content %}

        {% endblock %}

    </body>
</html>